# Manhattan Distance Calculator

Calculator de distanta Manhattan intre 2 vectori - implemented manual + scipy

```bash
python -m venv venv && venv\Scripts\activate
pip install -r requirements.txt
python src/main.py
```

Input: `data/input.txt` cu 2 vectori (numere separate prin spatii)
